# onboarding.yaml

appId: com.duckduckgo.mobile.ios
---

- copyTextFrom:
    id: "searchEntry"
- assertVisible:
    text: ${maestro.copiedText}
- evalScript: ${console.log('URL text:' + JSON.stringify(maestro.copiedText))}
- evalScript: ${throw new Error('URL text:' + JSON.stringify(maestro.copiedText))}